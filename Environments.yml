trigger: 
- main

pool: 
   vmImage: ubuntu-latest

jobs:
- deployment: VMDeploy
  displayName: Deploy to VM
  environment: AzDevOpsVM
   name:AzDevOpsVM
  strategy:
     runOnce:
        deploy:   
          steps:
            - script: echo "Hello world"
